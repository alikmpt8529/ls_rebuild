# rls リリースノート

## v0.3.0 — 2026-09-16

### 変更

- 更新時刻の表示を `ctime` 形式から `YYYY-MM-DD HH:MM` に変更した
- 拡張属性の確認を表示時から読み込み時に移した（`format_perm()` は文字列変換のみ）
- ファイル情報の取得を `stat` から `fstatat` に変更した
- 標準出力をバッファリングし、最適化オプション `-O2` を付けるようにした

### 使い方

```sh
make
./rls
```

出力例:

```
-rwxr-xr-x@ rls 2026-09-16 00:48
-rw-r--r--@ rls.c 2026-09-16 00:47
```

`make` した直後の1回目は、macOS が新しい実行ファイルを検査するため遅くなることがある。2回目以降が実際の実行時間です。

---

## v0.2.0 — 2026-09-16

### 追加

- 権限・ソート・一覧・エントリを別ファイルに分割した
- `Makefile` を追加した（`make` / `make clean`）
- Doxygen 形式のコメントを付けた
- `ファイル構成.md` を追加した
- `opendir` 失敗時にエラーを出して終了するようにした

### 変更

- ビルド方法を `gcc rls.c -o rls` から `make` に変更した

---

## v0.1.0 — 2026-09-15

カレントディレクトリのファイル一覧を、`ls` 風に表示するコマンドの最初のリリースです。

### 追加

- カレントディレクトリのファイルを一覧表示する
- 権限を `drwxr-xr-x` 形式で表示する（拡張属性がある場合は末尾に `@`）
- ファイル名と更新時刻を表示する
- `.` と `..` は表示しない
- 更新時間が新しい順に並べる

### 使い方

```sh
gcc rls.c -o rls
./rls
```

出力例:

```
-rwxr-xr-x@ rls Tue Sep 15 23:44:16 2026
-rw-r--r--@ rls.c Tue Sep 15 23:44:12 2026
```

### 既知の制限

- 表示対象はカレントディレクトリのみ（パス引数は未対応）
- 読み込み件数は最大 256 件
- `ls -l` にあるリンク数・所有者・グループ・サイズ・`total` 行は未実装
- 隠しファイル（`.` で始まる名前）は `.` / `..` 以外も表示する

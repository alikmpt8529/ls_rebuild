# rls リリースノート

## v0.2.0 — 2026-09-16

### 追加

- 権限・ソート・一覧・エントリを別ファイルに分割した
- `Makefile` を追加した（`make` / `make clean`）
- Doxygen 形式のコメントを付けた
- `ファイル構成.md` を追加した
- `opendir` 失敗時にエラーを出して終了するようにした

### 変更

- ビルド方法を `gcc rls.c -o rls` から `make` に変更した

---

## v0.1.0 — 2026-09-15

カレントディレクトリのファイル一覧を、`ls` 風に表示するコマンドの最初のリリースです。

### 追加

- カレントディレクトリのファイルを一覧表示する
- 権限を `drwxr-xr-x` 形式で表示する（拡張属性がある場合は末尾に `@`）
- ファイル名と更新時刻を表示する
- `.` と `..` は表示しない
- 更新時間が新しい順に並べる

### 使い方

```sh
gcc rls.c -o rls
./rls
```

出力例:

```
-rwxr-xr-x@ rls Tue Sep 15 23:44:16 2026
-rw-r--r--@ rls.c Tue Sep 15 23:44:12 2026
```

### 既知の制限

- 表示対象はカレントディレクトリのみ（パス引数は未対応）
- 読み込み件数は最大 256 件
- `ls -l` にあるリンク数・所有者・グループ・サイズ・`total` 行は未実装
- 隠しファイル（`.` で始まる名前）は `.` / `..` 以外も表示する
